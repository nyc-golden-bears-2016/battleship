<% if @current_game.winner_id == @current_user.id %>
  <h1>Well played, <%= @current_user.username.capitalize %>. You win!</h1>
<% else %>
  <h1>You lost the battle, <%= @current_user.username.capitalize %>, but did you lose the war?</h1>
<% end %>
